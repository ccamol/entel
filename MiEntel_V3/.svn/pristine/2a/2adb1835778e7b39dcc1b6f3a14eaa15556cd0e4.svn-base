/* Propiedad de Entel PCS. Todos los derechos reservados */
package com.epcs.venta.fidelizacion.delegate;

import org.apache.log4j.Logger;

import com.epcs.venta.fidelizacion.dao.BeneficioDAO;

/**
 * @author jlopez (I2B) en nombre de Absalon Opazo (Atencion al Cliente,
 *         EntelPcs)
 * 
 */
public class BeneficioDelegate {

    private static final Logger LOGGER = Logger
            .getLogger(BeneficioDelegate.class);

    private BeneficioDAO bDAO;

    public BeneficioDelegate() {
        this.bDAO = new BeneficioDAO();
    }

}
